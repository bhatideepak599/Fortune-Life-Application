// Loader.js
import React from 'react';
import './Loader.css'; // We'll add some CSS for the loader styling

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
